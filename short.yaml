---
setups:
  aws-sam:
    file: run.sh
    array_vars:
      params:
        pattern: "PARAM_.*"
        format: "ParameterKey={key},ParameterValue={value}"
        case: camelcase
    vars:
      - SAM_S3_BUCKET_DEPLOY_REGION
      - STACK_REGION
